const cardStyle = `.ik-card {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
    align-self: stretch;
    border-radius: 16px;
    background: var(--Schemes-On-Primary, #FFF);
    box-shadow: 0px 1px 4px 0px rgba(81, 43, 218, 0.10);
    padding: 24px;
}`
export default cardStyle;